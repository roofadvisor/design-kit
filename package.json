{
  "name": "dev-kit",
  "version": "2.1.1",
  "private": true,
  "description": "Playwright devDependency for kit/scripts' render gates (measure_render, verify_states, axe_audit, verify_focustrap, verify_rtl, ...). See README.md Notes.",
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
